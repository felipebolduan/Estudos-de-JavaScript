/* Tipos de dados primitivos -> String, number, undefined, null, boolean e symbol*/

const nome = 'Felipe'; // String
const idade = 23; // Number
const altura = 1.91; // Number
let nomeAluno; // Undefined = não aponta para lugar nenhum na memória;
let sobrenomeAluno = null; // Nulo = não aponta para local nenhum na memória;
const boolean = true; // Valor Booleano (Verdadeiro ou Falso);

console.log (typeof nome);
console.log (typeof idade, idade); // Mostra o tipo e valor
console.log (typeof altura);
console.log (typeof nomeAluno);
console.log (typeof sobrenomeAluno);
console.log (typeof boolean);